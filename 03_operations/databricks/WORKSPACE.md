# Azure Databricks workspace slot

**Empty on purpose.** When you have a live export, record:

- Workspace URL (`adb-….azuredatabricks.net` or Azure resource name)
- One real job name that matches a slice table (e.g. forecast snapshot)
- Cluster policy name, if any

Until then, jobs under `jobs/` stay **[Reconstructed]**. Catalog `edw_dev`
and ADLS `ashleydevlake` stay **[Verified]**.
