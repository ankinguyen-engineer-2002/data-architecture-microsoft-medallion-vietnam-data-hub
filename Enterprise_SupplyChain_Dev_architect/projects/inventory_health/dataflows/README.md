# Inventory Health Dataflows

This folder keeps temporary and reference Dataflow Gen2 assets separate from warehouse ETL SQL.

| Path | Purpose |
|---|---|
| `setup.md` | Dataflow setup runbook for EDW-to-Lakehouse workaround sources. |
| `drafts/` | Draft `.pq` files and API automation notes for inventory_health dataflows. |
| `templates/` | Reference Dataflow Gen2 template files copied from working forecast examples. |

Active warehouse ETL remains in `../etl/`.
