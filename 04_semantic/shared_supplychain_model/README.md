# Shared SupplyChain Semantic Model

This folder stores shared semantic-model source artifacts and notes.

Current model direction: `sc_control_tower`.

Important contract:

- shared dimensions come from `SupplyChain_Gold_Warehouse.Shared_DW`
- mart facts remain in their mart schemas
- semantic layer should not hide upstream data quality problems by patching ambiguous source rules

Historical files copied from the prior inventory mart semantic folder are preserved here as input material, not as guaranteed-current live model truth.

