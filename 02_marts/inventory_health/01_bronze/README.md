# Bronze Sources

Bronze files document upstream source objects.

- `01_enterprise_lakehouse/` contains active `Enterprise_Lakehouse` shortcut/source references.
- `02_supplychain_lakehouse/` is an empty marker for source separation; no active Inventory source currently uses `SupplyChain_Lakehouse`.
