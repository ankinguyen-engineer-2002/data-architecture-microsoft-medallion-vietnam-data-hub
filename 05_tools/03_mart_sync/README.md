# Mart Sync Tools

Read-only helpers for syncing local mart layer SQL from live Fabric Warehouse definitions.

## Scripts

| Script | Purpose |
|---|---|
| `sync_live_mart_layers.py` | Export/sync live mart layer definitions into `02_marts/`. |

## Safety

This hub is for local repo sync/export. Any live Fabric mutation still requires explicit same-conversation approval.
